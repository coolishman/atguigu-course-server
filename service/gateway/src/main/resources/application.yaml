server:
  port: 8888
spring:
  application:
    name: course-service-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: course-service-vod
          # 请求最终要被转发的地址
          uri: lb://course-service-vod
          predicates:
            - Path=/*/vod/**